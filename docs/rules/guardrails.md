# guardrails.md — 가드레일 문서

| 최종 갱신 | 최근 변경 |
|---|---|
| 2026-08-03 | `handoff/` 폐기 이관 — 에셋 파일 위치 배포 함정 |
| 2026-07-31 | 작업 PC kgblu 단독 확정 · git push 시점 가드레일 신설 |
| 2026-07-15 | 톡톡 비밀번호·마스터 값·Firestore 규칙 가드레일 이관 및 신설 |
| 2026-07-13 | 배포 절차(순서 고정) + pull 먼저·충돌 마커 금지·deploy.ps1 하드닝 규칙 추가 |
| 2026-07-10 | 가드레일 표 유지, 갱신정보 상단 이동 |

> 하면 안 되는 것·주의사항. 배포·보안·구조 변경 전 확인.

## 배포

### 배포 절차 (순서 고정 · 이 순서를 지킬 것)

1. **`git pull`** ← 제일 먼저, 파일을 배치·수정하기 **전에**. (로컬을 원격과 맞춘 clean 상태에서 시작)
   - `Already up to date.` 또는 조용한 업데이트면 정상. `CONFLICT`/`error:` 뜨면 **멈추고** 해결.
2. **파일 배치**: `Dinol_news_YYYYMMDD.html` → `news\YYYY\MM\`, `index.json` → 루트 덮어쓰기.
3. **대장 재생성**: `python scripts/build_published_urls.py` (→ "중복 등장 URL 없음" 확인).
4. **배포**: `./deploy.ps1 "add: M/D 브리핑"` (M/D는 실제 날짜로).

> ⚠️ **순서의 이유**: 파일을 먼저 얹고 pull하면(2→1 역순) 로컬·원격이 같은 파일을 고친 상태라 충돌난다. pull은 항상 clean 상태에서 맨 먼저. (2026-07-13 다운 사고가 이 역순 + 충돌 강행에서 발생, issues.md 참조)

### 가드레일

| 항목 | 가드레일 |
|---|---|
| push 권한 | Claude 컨테이너는 push 불가. 사용자가 로컬 수동, Claude는 outputs 전달만 |
| **pull 순서** | **작업 시작 전 `git pull` 먼저**(파일 배치·수정 전에). pull에서 `CONFLICT`/`error:` 뜨면 그 자리서 멈추고 해결 |
| **작업 PC** | **kgblu 단독 사용 확정(2026-07-31). jrdo 사용 안 함.** 두 PC 혼용에서 오던 pull 충돌 위험은 해소. 만약 다시 다른 PC를 쓰게 되면 반드시 `git pull` 먼저, 로컬이 원격보다 뒤처진 채로 배포 금지(히스토리 갈라져 충돌) |
| **git push 시점** | **동기화 목적의 상시 push 불필요**(PC 1대 운영). push는 ①Claude가 원격 파일을 raw URL로 읽어야 할 때 ②장기 백업이 필요할 때만. `astro` push는 상용 배포가 아님(GitHub Pages는 `main`만 빌드) |
| **충돌 마커** | `<<<<<<<` `=======` `>>>>>>>` 가 파일에 남은 채 배포 **절대 금지**. index.json에 섞이면 유효 JSON이 깨져 사이트가 "여는 데 문제가 있어요"로 다운. 하드닝 deploy.ps1이 자동 차단 |
| **deploy.ps1** | 2026-07-13 하드닝: pull 자체를 하지 않고 **fetch로 원격 확인만** → 로컬이 뒤처짐/갈라짐, 충돌 마커, push 실패 시 **자동 중단**. 중단 메시지 뜨면 원인 해결 후 재실행<br>**2026-08-02 추가: `main` 전용.** 비-main에서 실행하면 즉시 중단(exit 1). 8/1에 checkout 실패 후 그대로 실행돼 `astro`에 배포 커밋이 들어간 사고(`d696098`) 방지. 파일은 **UTF-8 BOM**으로 저장 — PowerShell 5.1은 BOM 없는 `.ps1`을 cp949로 읽어 한글이 파싱 시점에 깨진다 |
| **daily_import.py** | `[PASS] 전체 통과`가 떠야 커밋 대상. 중간 게이트에서 끊긴 채 커밋하지 않는다(7/31·8/1 재발). 이미 추출된 날짜 재생성은 `--redo` — 임시 디렉터리 선추출 후 검증 통과 시에만 교체하며, 실패 시 카드 8개+대장을 롤백한다. 대상 카드 8개가 **HEAD와 동일한 clean 상태**여야 실행된다(사용자 미커밋 수정 덮어쓰기 방지) |
| **에셋 파일 위치** | `dinol.css`·`dinol.js`·`dinol-firebase.js` 는 반드시 **`assets\` 안에** 덮어쓴다. 루트에 잘못 저장하면 HTML이 읽는 `assets/` 파일은 구버전 그대로라 배포해도 화면이 안 바뀐다. 루트 중복본은 삭제.<br>JS 수정이 안 먹힐 때 진단 순서 — ①`Select-String -Path "assets\dinol-firebase.js" -Pattern "고친로직"` ②`(Get-Item ...).Length` ③`git status`(clean이면 파일이 안 바뀐 것) ④배포 후 **레거시 브리핑 페이지(`news/2026/MM/`)** 콘솔에서 `fetch("../../../assets/dinol-firebase.js?v="+Date.now())` 로 라이브 검증. Astro 페이지는 URL 깊이가 다를 수 있으므로 **현재 페이지 기준으로 상대 경로를 다시 계산**한다 |
| 폼 변경 | 상단 폼 바꾸면 template + 모든 브리핑 HTML 전부 수정 |
| 배포 전 | `git status`로 스테이징 확인(파일 많을 때 누락 주의) |
| Firestore 규칙 | git 아님 → Firebase 콘솔 붙여넣기. 글자수·구조 바꾸면 규칙도 함께 |

## 데이터·보안

| 항목 | 가드레일 |
|---|---|
| 비밀번호 | 클라이언트 SHA-256(소프트). 운영자 481516도 "표시"용, 진짜 보안엔 사용 금지 |
| 요금제 | Spark 무료(읽기 5만/일, 저장 1GB). 트래픽 늘면 페이지네이션 검토 |
| 보관 | 방명록·댓글 영구 보관(자동 삭제 없음) |

## 코드 검증 (배포 전)

| 항목 | 방법 |
|---|---|
| JS | `node --check` + 핵심 로직 노드 재현 테스트 |
| CSS | 중괄호 짝 확인 |
| 치환 반영 | grep로 실제 반영 재확인(이스케이프 오탐 주의) |
| Firebase | 컨테이너 실행 불가 → 정적·드로어는 Playwright, 실동작은 배포 후 |

## 콘텐츠·브랜딩

| 항목 | 가드레일 |
|---|---|
| 브리핑 | URL 중복·목록/홈 URL 금지, em대시 금지, 저작권(요약·링크만) |
| 운세/놀이 | 특정 직군 비하 금지(애정 티키타카), 부정 항목은 유머로. 상세 콘텐츠 기준은 `reference/fortune-schema.md` §5 |
| 브랜드 | "디놀" 유지, 하위 코너명만. 유틸(도구)·놀이(재미) 성격 안 섞음 |


_git 이력이 상세 버전 관리를 대신함._

---

## 톡톡 인증 · Firestore 보안

| 항목 | 가드레일 |
|---|---|
| 비밀번호 저장 | **평문 저장 금지.** SHA-256 해시만 저장·대조 |
| 클라이언트 검증의 한계 | 브라우저 JS의 검증은 **UX 편의일 뿐 보안 경계가 아니다.** 실제 방어는 **Firestore 보안 규칙**이 한다. 규칙 없이 클라이언트 체크만 믿지 말 것 |
| **운영자 권한** | ⛔ **코드에 마스터 비밀번호를 두지 않는다.** 운영자 판별은 **Firebase Auth 세션**으로만 한다(`dinolAdmin.login()`). 클라이언트 상수·해시 어느 쪽도 금지 — 공개 JS라 평문은 즉시, 해시는 브루트포스로 뚫린다 |
| 운영자 계정 | Firebase 콘솔 Authentication에 전용 계정 1개. **UID를 `firestore.rules`에 박아 권한 부여**. 폐기된 값(`481516`)은 이미 공개됐으므로 **어떤 비밀번호로도 재사용 금지** |
| Firestore 규칙 | git 아님 → **Firebase 콘솔에 직접 게시.** 글자수·구조 변경 시 규칙도 함께(4곳: 글 create/update, 댓글 create/update) |
| 규칙 범위 | `comments` 서브컬렉션 + `commentCount` 갱신 규칙 포함 |
| App Check | reCAPTCHA v3 **Enforce**(콘솔에서 강제). 규칙과 별개 레이어 |
