# dev-history.md — 개발 히스토리

| 최종 갱신 | 최근 변경 |
|---|---|
| 2026-07-10 | 상세 정보 복원(압축 해제), 순서 재정렬, 상태 라벨화 |

> 개발한 기능. 완료 시 표에 행 추가. 상태: 완료 / 진행중 / 보류 / 예정.
> **원칙: 간결함보다 정확·완전한 정보 우선. 길어도 됨.**

## 1. 로직·구조

| 날짜 | 기능 | 파일 | 내용(상세) | 상태 |
|---|---|---|---|---|
| 2026-07-06 | 공용 CSS/JS 분리 | dinol.css, dinol.js, dinol-firebase.js | 브리핑 HTML마다 중복되던 스타일·스크립트를 공용 에셋으로 분리. 각 브리핑은 `../../../assets/`로 참조(브리핑이 news/2026/MM/ 3단계 깊이라 상대경로 3단). 유지보수 시 파일 1개만 고치면 전체 반영 | 완료 |
| 2026-07-08 | 브리핑 자동화 | scripts/build_briefing.py, Claude 루틴 | ①로컬: cards.json 읽어 template 복제→날짜별 HTML 생성 + index.json 갱신 + 자체검증(8카드·한글라벨·em대시 없음). Python 필요(사용자 PC 미설치 상태라 미실행). ②루틴: 매일 10시 Anthropic 서버에서 실행, WebSearch 스니펫만으로 생성(뉴스 도메인 WebFetch는 루틴 환경서 403 차단, raw.githubusercontent만 허용). 둘 다 push는 사람이 수동, 상호 보완 | 완료 |
| 2026-07-10 | 배포 스크립트 | deploy.ps1 | `./deploy.ps1` 한 줄로 pull→add→commit→push. 커밋 메시지 자동(브리핑+오늘날짜) 또는 인수 지정. Set-Location으로 레포 이동 포함. 최초 1회 ExecutionPolicy RemoteSigned 필요 | 완료 |
| 2026-07-10 | index 리다이렉트 | index.html | 방문자가 루트 접속 시 index.json의 최신 날짜를 읽어 최신 브리핑으로 자동 이동. 리다이렉트 실패 시 경고 아이콘+재시도 버튼 표시(에러 페이지 겸용) | 완료 |

## 2. 카드·드로어 (팝업) — dinol.js

| 날짜 | 기능 | 내용(상세) | 상태 |
|---|---|---|---|
| 2026-07-08 | 6필드 팝업 | 카드 클릭 시 드로어 오픈. 6개 data 속성으로 채움: data-summary(요약), data-points(핵심 인사이트 3개, `\|`구분), data-designer(디자이너 관점), data-impact-score(실무 영향도 1~5 정수 별점), data-recommend(활용 추천), data-comment(큐레이션 노트 2~3문단, 실제 `\n`). 영문 기사는 각 -kr 짝 속성도 보유 | 완료 |
| 2026-07-10 | KR/EN 토글 | 영문 기사(data-title-en+번역 있음): KR/EN 버튼으로 언어 전환(기본 KR). 한글 기사: KR 버튼만 활성 표시, EN 버튼 숨김(display:none), 토글 기능 없음. hasEn 판정은 카드에 `.thumb-en` 배지 존재 여부. dinol.js ~181행 분기 | 완료 |
| 2026-07-10 | 원문 CTA | 드로어 하단 "원문 보기 →"(.drawer-cta) = 카드 href로 이동. bg #2c2c48로 KR/EN 활성 버튼(.lang-btn.active)과 동일 톤 통일. 활성 lang 버튼에도 호버 색(#34345a) 적용 | 완료 |

## 3. 톡톡 (방명록·댓글·좋아요) — dinol-firebase.js

| 날짜 | 기능 | 내용(상세) | 상태 |
|---|---|---|---|
| 2026-07-09 | Firestore 구조 | guestbook/{postId} = {nick, body, pwHash, createdAt, commentCount}. 하위 comments/{commentId} = {nick, body, pwHash, createdAt}. likes/{id} = {count}. asia-northeast3, Spark(무료). App Check reCAPTCHA v3(Enforce). 댓글 지연 로드(펼칠 때 getDocs) | 완료 |
| 2026-07-09 | 비밀번호 | 클라이언트 SHA-256 해시로 저장·검증. 수정/삭제 시 입력 해시 == 저장 pwHash 또는 마스터(481516)면 허용. Firestore 규칙에서 pwHash.size()==64 강제 | 완료 |
| 2026-07-10 | 리스트형 UI | 네이버식 개편. 박스 카드 제거→평평한 리스트(border-bottom 라인 구분). 구조: 아바타(이모지 해시) + 닉네임·시간 한 줄(gb-entry-head) + 본문. 댓글도 동일 구조(작은 아바타 gb-avatar-sm) | 완료 |
| 2026-07-10 | 댓글 진입 플로우 | 글마다 액션행: "댓글쓰기"·"댓글 N ▼". 댓글쓰기→입력창(컴포저)만 노출(펼친 상태면 목록 위에). 댓글 N ▼→목록 토글(로드). 댓글 0이면 "댓글 N" 토글 자체 숨김(댓글쓰기만). 화살표는 펼치면 회전 | 완료 |
| 2026-07-10 | 옵션 메뉴 | 각 글·댓글 우상단 세로 점 3개(⋮). 클릭 시 수정/삭제 메뉴. 메뉴 bg #2a2a2c(이모지 피커와 동일), ⋮와 8px 간격, 오른쪽 끝 정렬, 좌우 여백 균형(우측 살짝 더) | 완료 |
| 2026-07-10 | 이모지 | 48개(CEMOJI 배열). 본문에 입력값 있을 때만 이모지 버튼 노출(기본 CSS display:none, JS로 토글). 팝업은 버튼 아래(top:100%). 데스크톱 8열×34px, 모바일 6열(화면 맞춤, 짤림 방지). 닉네임·비번엔 미삽입, 본문에만 | 완료 |
| 2026-07-10 | 운영자K | 비번칸에 481516(숫자 6자리) 입력→닉네임 없이 "운영자K"+아바타 ✨로 등록. 방명록·댓글 둘 다. avatar()가 "운영자K"→✨ 특수처리. 원래 "관리자K"였으나 커뮤니티 친근 톤 위해 변경 | 완료 |
| 2026-07-10 | 단일 컴포저 | openComposer(pid)에서 다른 모든 글의 composerOpen=false 처리 후 해당 글만 true. 즉 여러 글에서 댓글쓰기 눌러도 최종 클릭한 1개 입력창만 열림 | 완료 |
| 2026-07-10 | 상단 폼 재구성 | 카운터(0/1000)를 텍스트영역 안 우하단으로. 이모지+등록을 툴바(gb-tbar)로. 등록 버튼: 비활성 회색, 활성 시 주황 bg(#FF7F3E)+흰 텍스트. 이 마크업은 template+모든 브리핑에 복제됨 | 완료 |
| 2026-07-09 | 공유 | 웹 표준 API 3단계 폴백: navigator.share(모바일 네이티브 공유 시트)→navigator.clipboard(PC 링크 복사+토스트)→토스트. 커스텀(SNS버튼·OG태그·카카오SDK)은 논의만, 미구현 | 완료 |
| 2026-07-09 | 포인트 컬러 | 오렌지 #FF7F3E. 댓글 N·등록 버튼 등 강조에 사용 | 완료 |
| 2026-07-10 | 입력창 톤 통일 | 상단 폼·댓글 폼 입력칸을 동일 중립 그레이(bg #242426, 테두리 #343436, 포커스 #484848)로. 남색끼 제거. 폰트 데스크톱 15px, 모바일 16px(iOS 포커스 줌 방지). 카드·드로어는 사이트 남색 정체성 유지(입력창만 중립) | 완료 |
| 2026-07-10 | 비번 눈 아이콘 | 브라우저 기본 비밀번호 눈/지우기 아이콘(::-ms-reveal, ::-ms-clear) CSS로 숨김. Edge 대상(Chrome은 원래 없음) | 완료 |

## 4. 브리핑 페이지 UI 요소 (template.html)

| 날짜 | 기능 | 내용(상세) | 상태 |
|---|---|---|---|
| 2026-07-06 | 폰트 | Cormorant Garamond 300(디자인 놀이터) + Noto Sans KR 700(AI & Design News 46px) + Noto Serif KR 300(본문) | 완료 |
| 2026-07-06 | 헤더 4줄 | "디자인 놀이터"(자간 5px #aaa) / "AI & Design News"(46px white) / `.header-sep`(1px×24px 구분선) / 날짜(11px 자간4px #888) | 완료 |
| 2026-07-06 | 읽음 상태 | 카드 열람 시 `.card-title.read`(weight 450, #a3a3a3). localStorage `dinol_read_urls`(URL 배열)에 저장→재방문에도 유지. getReadUrls/markAsRead/applyReadState | 완료 |
| 2026-07-06 | 카드 검증 JS | DOMContentLoaded 시: 중복 URL→빨간 테두리+⚠배지, 빈 링크→노란 테두리+⚠배지, 이슈 시 하단 고정 배너 | 완료 |
| 2026-07-06 | 아카이브·카톡 버튼 | 카드 그리드 끝에 `.archive-cta-wrap`(세로, gap 12px). "AI & Design 아카이브"(.archive-cta, #c9ccd4, ./archive.html) + "디자인놀이터 오픈채팅방"(.kakao-cta, 카카오옐로우 #FEE500, open.kakao.com/o/g3XICwx). 둘 다 width 240·min-height 48·검정 텍스트 | 완료 |
| 2026-07-06 | 맨 위로 버튼 | `.to-top`(#toTop). 모바일(≤580px)만, 스크롤 600px+ 시 `.show` 페이드인, 클릭 시 상단 스무스 스크롤 | 완료 |
| 2026-07-06 | 그라디언트 | 썸네일 배경 클래스(g-indigo/violet/teal/crimson/forest/slate/amber/plum/olive/navy/rust 등 12종) + `.noise` + 중앙 라벨 | 완료 |
| 2026-07-06 | 푸터 | © 저작권 3줄(13px, 안내문 `<br>` 한 덩어리) + 개인정보처리방침 링크(16px 밑줄). 아카이브는 위 pill 버튼으로 대체 | 완료 |
| 2026-07-06 | AdSense(미완) | `<head>` 자동광고 스크립트 `ca-pub-XXXXXXXXXXXXXXXX` + ads.txt. **TODO: 실제 게시자 ID로 교체(template·브리핑·archive·privacy 4곳 + ads.txt)** | 진행중 |

| 2026-07-10 | 좋아요 key 날짜분리 | dinol-firebase.js | likeKey에 브리핑 날짜(Dinol_news_YYYYMMDD) 접두어 추가 → `20260710_<url>`. 같은 URL이 다른 날 재등장해도 좋아요 분리(이월 차단). ⚠️기존 URL-only 키 좋아요는 리셋됨 | 완료 |
| 2026-07-10 | 발행 URL 대장 | published_urls.json, scripts/build_published_urls.py | 발행된 카드 URL을 {URL:최초발행일}로 저장. 스크립트로 전체 브리핑에서 재생성. 중복 발행 방지의 원본 | 완료 |

## 5. 향후 로드맵 (미구현·기획)

| 날짜 | 코너 | 항목(상세) | 난이도 | 상태 |
|---|---|---|---|---|
| 2026-07-10 | 운세 | 디자이너 오하아사. 오늘의 확률(야근·외주·한번에통과·3회+수정·주목/채택·리젝), 궁합(팀장·동료·기획자·개발자·대표·클라이언트), 오늘의 기운(행운의 색 HEX·행운의 폰트·오늘의 조언). 부정 항목은 유머로 승화. 루틴으로 매일 생성. 공유 카드가 바이럴 핵심 | ★☆☆ | 예정 |
| 2026-07-10 | 유틸 | 확정 7종(전부 브라우저 JS, DB 불필요): 컬러피커, 추천 컬러 조합(색상환 계산), 팔레트 추출(이미지→색5개), HEX↔RGB↔HSL 변환, 그라디언트 생성기, 대비(명암) 검사기, 한/영 립숨. 보류: 파비콘·비슷한폰트찾기·목업(범위·정확도 문제) | ★~★★ | 예정 |
| 2026-07-10 | 놀이 | 유입·재방문·공유 목적. 디자이너 유형 테스트(MBTI식, 바이럴 1순위), 오늘의 밸런스 게임(투표+실시간 통계, Firebase 활용), 발표자/점심/커피 룰렛(팀 단위 유입). 디자이너 맥락으로 비틀기, 유틸과 별도 방 | ★★☆ | 예정 |
| 2026-07-10 | 네비게이션 | 디놀 상위 브랜드 아래 탭: 브리핑·운세·유틸·놀이·톡톡. 로고 아래 탭 네비, 현재 페이지 오렌지 활성. 헤더가 모든 브리핑에 복제라 공통 헤더 관리 전략 함께 고민 필요 | ★★☆ | 예정 |

_git 이력이 상세 버전 관리를 대신함._
